kind: pipeline
name: default
type: docker

steps:
- name: docker
  image: plugins/docker
  when:
    branch:
      - 4-create-drone-yml
  settings:
    username:
      from_secret:
        docker_username
    password:
      from_secret:
        docker_password
    repo: scr1pter/devops-challenge
    tags: latest
